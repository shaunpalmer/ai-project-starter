import 'dotenv/config';
import fs from 'fs';
import path from 'path';
import { OpenAI } from 'openai';

const NVIDIA_API_KEY = process.env.NVIDIA_API_KEY;
const NVIDIA_BASE_URL = 'https://integrate.api.nvidia.com/v1';
const NVIDIA_MODEL = 'Qwen3.5-122B-A10B-Obsidian';

const openai = new OpenAI({
  apiKey: NVIDIA_API_KEY,
  baseURL: NVIDIA_BASE_URL,
});

## npm install openai dotenv  
/*That’s all you needed: endpoint variable, model variable, 
and private API-key variable wired into Glow. 
The next step is to paste this opening section over the current imports and client setup,
then run the optimiser once manually before scheduling it nightly.
*/

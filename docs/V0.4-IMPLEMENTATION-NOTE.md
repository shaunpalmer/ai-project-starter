Implementation note: v0.4 is intentionally split into a proven controller slice followed by generated-project integration. The first slice proves engineering defaults, deterministic skill binding, consolidated WordPress rules, VCS control, and regression safety before those controls are copied into generated product projects.